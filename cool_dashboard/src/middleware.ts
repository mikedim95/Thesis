/* export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    "/profile",

    "/tables",
    "/forms/:path*",
    "/settings",
    "/chart",
    "/ui/:path*",
    "/",
  ],
  pages: {
    signIn: "/auth/login",
  },
};
 */
